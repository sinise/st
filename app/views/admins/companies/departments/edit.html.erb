<div class="page-header">
	<h1>Ret department</h1>
</div>

<div class="row">
	<div class="col-md-6">
		<%= form_for([@company, @department], url: admins_company_department_path, method: :put) do |f| %>
			<%= render 'admins/companies/departments/form', :f => f %>
		<% end %>
	</div>
</div>
