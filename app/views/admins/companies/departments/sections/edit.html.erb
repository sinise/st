<div class="page-header">
	<h1>Ret section</h1>
</div>

<div class="row">
	<div class="col-md-6">
		<%= form_for([@company, @department, @section], url: admins_company_department_section_path, method: :put) do |f| %>
			<%= render 'admins/companies/departments/sections/form', :f => f %>
		<% end %>
	</div>
</div>
