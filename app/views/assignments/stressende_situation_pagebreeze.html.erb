
<div class="page-header">
	<h1>Stressende situationer</h1>
</div>

<div class="well well-tight">
  <h3>Opgavebeskrivelse</h3>

  <p>Det er vigtigt, at en indsats i forhold til stress er specifik og m&aring;lrettet.</p>

  <p>T&aelig;nk derfor p&aring; en konkret situation fra sidste uge, hvor du i s&aelig;rlig grad blev p&aring;virket negativt.</p>

  <p>Beskriv sitautionen s&aring; konkret som muligt med tid, sted, hvem der var involveret. Undg&aring; at komme med bud p&aring; evt. &aring;rsagen - n&oslash;jes med at beskrive det, der skete.</p>
</div>

<%= form_for(@assignment, :url => '/assignments/' << @assignment._id << '/save_type1/', :method => :put) do |f| %>

	<div class="row">
	<div class="col-md-4">

		<%= f.label :q1, 'Hvor var du henne?', :class => "form-label-tight" %>
		<%= f.text_area :q1,  :value => @assignment.result['q1'], :class => "form-control"  %>

		<%= f.label :q2, 'Hvon&aring;r var det (dato, hvilken dag, klokkeslet)&nbsp;', :class => "form-label-tight" %>
		<%= f.text_area :q2,  :value => @assignment.result['q2'], :class => "form-control"  %>

		<%= f.label :q3, 'Hvem var til stede ud over dig', :class => "form-label-tight" %>
		<%= f.text_area :q3,  :value => @assignment.result['q3'], :class => "form-control"  %>

		<%= f.label :q4, 'Hvad var din opgave? (Hvad &oslash;nskede du at bidrage med i situationen&nbsp;- hvad ville du gerne have, der skete?)', :class => "form-label-tight" %>
		<%= f.text_area :q4,  :value => @assignment.result['q4'], :class => "form-control"  %>

		<%= f.label :q5, 'Beskriv, hvad der skete:', :class => "form-label-tight" %>
		<%= f.text_area :q5,  :value => @assignment.result['q5'], :class => "form-control"  %>

		<%= f.label :q6, 'Hvilke tanker gik gennem dit hoved?', :class => "form-label-tight" %>
		<%= f.text_area :q6,  :value => @assignment.result['q6'], :class => "form-control"  %>

		<%= f.label :q7, 'Beskriv, hvordan det p&aring;virkede det dig? (hvordan kunne du m&aelig;rke, at det p&aring;virkede dig? I kroppen, som en f&oslash;lelse, eller?', :class => "form-label-tight" %>
		<%= f.text_area :q7,  :value => @assignment.result['q7'], :class => "form-control"  %>
  </div>
  </div>

	<div class="form-actions">
		<%= f.submit "Gem opgave", :class => "btn btn-primary btn-lg" if current_user && !@assignment.submitted? %>

		<%= link_to 'Submit', '/assignments/' << @assignment._id << '/submit', \
				        :class => 'btn btn-primary', method: :put, \
				        data: { confirm: 'To submit the assignment?' }  if current_user && !@assignment.submitted? %>
	</div>

	<%= f.hidden_field :version,  :value => '1.0'%>

<% end %>


